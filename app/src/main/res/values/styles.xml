<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="buttonStyle">@android:style/Widget.Button.Small</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="android:textColorPrimary">@color/v2ex_normal_white</item>
        <item name="colorControlNormal">@color/v2ex_normal_white</item>
        <item name="vpiTabPageIndicatorStyle">@style/CustomTabPageIndicator</item>
        <item name="alertDialogTheme">@style/V2ex.AlertDialog</item>


    </style>

    <style name="V2exAppTheme" parent="AppTheme"/>

    <style name="CustomTabPageIndicator" parent="Widget.TabPageIndicator">
        <item name="android:background">@drawable/custom_tab_indicator</item>
        <item name="android:textAppearance">@style/CustomTabPageIndicator.Text</item>
        <item name="android:textColor">@drawable/selector_tab_text</item>
        <item name="android:textSize">16sp</item>
        <!-- <item name="android:divider">@drawable/custom_tab_indicator_divider</item>-->
        <item name="android:dividerPadding">10dp</item>
        <item name="android:showDividers">none</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:paddingRight">10dp</item>
        <item name="android:fadingEdge">horizontal</item>
        <item name="android:fadingEdgeLength">8dp</item>
        <item name="android:width">80dp</item>
    </style>

    <style name="V2ex.AlertDialog" parent="@style/Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@android:color/holo_red_light</item>
    </style>

    <style name="V2ex.ProgressDialog" parent="@style/Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@android:color/holo_red_light</item>
    </style>

    <style name="CustomTabPageIndicator.Text" parent="android:TextAppearance.Medium">
        <item name="android:typeface">monospace</item>
        <item name="android:textColor">@drawable/selector_tab_text</item>
    </style>
    <style name="V2exAppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="V2exAppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>


</resources>
